# 開発タスクリスト (task.md)

本プロジェクトの開発進捗を管理するためのTODOリストです。

---

- `[ ]` **1. 最小位相EQエンジン（DSP）の実装**
  - `[ ]` ゼロ位相処理（OLA）および先読みバッファの廃止
  - `[ ]` 64-bit精度 Topology-Preserving Transform (TPT) SVF モジュールの実装 (`MinimumPhaseEQ.h/cpp` の新規作成)
  - `[ ]` ローカット、ハイカット、および4バンドベル型EQの直列カスケード処理の構築
  - `[ ]` 近接バンド間での相互干渉を低減するための極零点パラメータ最適化（フィッティング）の導入

- `[ ]` **2. 超高解像度非FFTアナライザー（DSP）の実装**
  - `[ ]` Burg法による自己回帰（AR）モデル係数の推定ロジックの実装 (`AnalyzerDSP.h/cpp` の再構築)
  - `[ ]` AR係数から任意の周波数のパワースペクトル密度（PSD）を解析的に算出する関数の実装
  - `[ ]` Gaborフレーム分解および簡易ベイズ推定による定常ノイズの分離と倍音抽出スレッドの実装

- `[ ]` **3. プロセッサコア（PluginProcessor）の統合**
  - `[ ]` LowCut/HighCut、4バンドBell、Diff（Listen Diff）、Bypassパラメータのバインド
  - `[ ]` ゼロレイテンシー化に伴うDAWへのレイテンシー申告の修正（0サンプル、またはアナライザバッファ用の極小値）
  - `[ ]` Diff処理（`delayedDry - wet`）の最小位相EQ仕様への適合（遅延バッファの調整）

- `[ ]` **4. AVX2 SIMD 最適化**
  - `[ ]` TPT SVF カスケードにおけるステレオ左右チャンネル同時演算のAVX2化
  - `[ ]` Burg法における自己相関ループ等のボトルネック演算のAVX2化

- `[ ]` **5. GUI描画とグラフィックス最適化**
  - `[ ]` 表示ピクセル幅に合わせた適応的周波数プロット（周波数ワーピング）の実装 (`FreqResponseDisplay.h/cpp` の変更)
  - `[ ]` 低域ズーム時の動的0.02Hz/1Hz超高解像度レンダリングロジックの実装
  - `[ ]` JUCE 8のDirect2Dレンダラー向けに `setBackupEnabled(false)` を適用したGPUオンリーテクスチャ描画の実装

- `[ ]` **6. 動作検証と音響特性テスト**
  - `[ ]` PluginDoctor等を用いた周波数特性・位相特性・群遅延の測定と、にじみがないことの確認
  - `[ ]` プロファイラを用いたCPU/GPU負荷測定と、Intel統合GPU上での描画パフォーマンス検証
