# 音響工学・信号処理技術リサーチノート (Research Notes)

最強のミニマムフェーズEQおよび超高解像度アナライザーの開発に向け、必要となる音響工学・数理的知見を体系的に整理しました。

---

## 1. 最小位相（Minimum Phase）フィルタと位相特性の物理

### 1.1 振幅応答と位相特性の関係（Kramers-Kronigの関係式 / ヒルベルト変換）
物理的に安定で因果的なLTI（線形時不変）システムにおいて、振幅応答と位相特性は独立して決めることはできません。最小位相システムでは、周波数特性 $H(\omega) = |H(\omega)| e^{j \phi(\omega)}$ において、対数振幅特性 $\ln |H(\omega)|$ と位相特性 $\phi(\omega)$ の間に次の**ヒルベルト変換（Hilbert Transform）**の関係が成り立ちます。

\[
\phi(\omega) = -\frac{1}{\pi} \text{P.V.} \int_{-\infty}^{\infty} \frac{\ln |H(\lambda)|}{\omega - \lambda} d\lambda
\]
*※ P.V. はコーシーの主値*

この関係式により、目標の振幅特性（イコライザーのカーブ）を決定した瞬間に、**「数学的に最小の位相遅れ（Minimum Phase Shift）」**を持つ位相応答 $\phi(\omega)$ が一意に定まります。

### 1.2 ポストエコーと群遅延（Group Delay）のメカニズム
イコライザーで特定の周波数を急峻にカット（またはブースト）すると、過渡応答としてインパルス応答が時間軸上で引き延ばされ、**ポストエコー（リングテール）**が発生します。

- **プリエコーとポストエコー**:
  - **線形位相（Linear Phase）**: インパルス応答が時間軸上で左右対称になるため、音の立ち上がりより前に「プリエコー」が発生し、トランジェント（打楽器のアタックなど）を損ないます。
  - **最小位相（Minimum Phase）**: 因果性により、エネルギーが時間軸の未来方向（音の後ろ側）にのみ集中します（プリエコーはゼロ、ポストエコーのみ発生）。
- **ポストエコーの持続時間**:
  フィルターの鋭さ（Q値）と中心周波数 $\omega_0 = 2\pi f_0$ に対し、ポストエコーの振幅エンベロープは指数関数 $e^{-t/\tau}$ で減衰します。この時定数 $\tau$ は以下で表されます。
  
  \[
  \tau = \frac{2Q}{\omega_0} = \frac{Q}{\pi f_0}
  \]
  
  Q値が極めて高い（例：Q=100）ディープなノッチカットでは $\tau$ が大きくなり、ポストエコーの持続時間が長くなります。
- **群遅延（Group Delay）**:
  信号のエンベロープが遅延する時間 $D_g(\omega)$ は、位相応答 $\phi(\omega)$ の角周波数微分で定義されます。
  
  \[
  D_g(\omega) = -\frac{d\phi(\omega)}{d\omega}
  \]
  
  最小位相システムは、あらゆる因果的システムの中で、すべての周波数において**群遅延の総和が数学的最小**になります。

### 1.3 TPT (Topology-Preserving Transform) SVF 構造による演算誤差排除
一般的なデジタルフィルターは双一次変換を用いてアナログ回路を差分方程式（Direct Form 1 / 2 等）に変換しますが、これらは超低域や高Q値設定で係数の丸め誤差に極めて敏感になり、発振や位相にじみ（にじみノイズ）を誘発します。
本設計で用いる**TPT SVF**構造は、アナログ状態変数フィルタ（SVF）の積分器を「台形積分（Trapezoidal Integration）」によってデジタル化し、フィードバックループ内のディレイ（遅延）を含まない形で代数的に解いたものです。

| 特性 | 通常の Direct Form 1 / 2 | TPT (Topology-Preserving Transform) |
| :--- | :--- | :--- |
| **数値的安定性** | 低域および高Q（Q > 50）で演算ノイズ・にじみが発生しやすい | 極めて安定（Q=120でも丸め誤差がほぼ発生しない） |
| **周波数スイープ** | 係数の急激な変化でプチプチ音（トランジェント歪み）が発生 | 状態変数が物理回路と1対1対応しているため、動的スイープが極めて滑らか |
| **内部演算精度** | 32-bit float では低域で精度不足になりやすい | 64-bit double 精度によりにじみ空間を0.02Hz内に完全局所化 |

---

## 2. 自己回帰（AR）モデルによる高解像度パラメトリックスペクトル推定

### 2.1 Burg法（Burg's Method）の数学的定式化
FFTを用いたスペクトル解析は、有限の窓長（ハニング窓など）によって時間-周波数解像度が制約されます（ハインゼベルグの不確定性原理）。
一方、自己回帰（AR）モデルは、信号が以下の線形予測モデルから生成されていると仮定します。

\[
x_n = -\sum_{k=1}^{p} a_k x_{n-k} + e_n
\]
*※ $a_k$ はAR係数、$p$ はモデル次数、$e_n$ は予測誤差（イノベーション系列）*

**Burg法**は、順方向予測誤差 $f_{m,n}$ と逆方向予測誤差 $b_{m,n}$ の二乗和の平均を最小化するように、反射係数（PARCOR係数） $k_m$ を順次決定します。

\[
E_m = \sum_{n=m}^{N-1} \left( |f_{m,n}|^2 + |b_{m,n}|^2 \right)
\]

各次数 $m$ における反射係数 $k_m$ の更新公式は以下の通りです。

\[
k_m = \frac{-2 \sum_{n=m}^{N-1} f_{m-1,n} b_{m-1,n-1}^*}{\sum_{n=m}^{N-1} \left( |f_{m-1,n}|^2 + |b_{m-1,n-1}|^2 \right)}
\]

得られた反射係数から、**Levinson-Durbin再帰関係**を用いてAR係数 $a_k$ を更新します。

\[
a_{m,i} = a_{m-1,i} + k_m a_{m-1,m-i}^* \quad (i=1, 2, \dots, m-1)
\]
\[
a_{m,m} = k_m
\]

#### Burg法が本システムに適している理由:
1. **絶対安定性**: 反射係数 $|k_m| < 1$ が常に保証されるため、推定されるARフィルターの極が必ず単位円の内側に配置され、スペクトルが異常発振しません。
2. **高分解能**: 非常に短いサンプル長（例: 数十ms）からでも、低域の鋭い単一正弦波のピークを極めて鮮明（0.02Hz解像度）に捉えることができます。

### 2.2 解析的スペクトルプロットと周波数ワーピング
AR係数 $a_k$ と予測誤差の分散 $\sigma_e^2$ から、角周波数 $\omega$ におけるパワースペクトル密度（PSD） $P(\omega)$ は、FFTを使用せずに以下の数式から直接計算できます。

\[
P(\omega) = \frac{\sigma_e^2}{\left| 1 + \sum_{k=1}^{p} a_k e^{-j k \omega} \right|^2}
\]

この数式に、表示画面のピクセル位置に対応する対数周波数点 $\omega_i = 2\pi f_i / f_s$ を代入することで、**無駄な周波数の計算を一切行わずに、表示ピクセルに完全追従した高解像度曲線（周波数ワーピングプロット）**が得られます。

---

## 3. Gaborフレームとベイズ推論による時間-周波数分解

### 3.1 Gaborフレームによる時間-周波数平面のタイリング
音声信号 $x(t)$ を、ガウス関数などの時間-周波数窓関数 $g(t)$ を時間軸方向に $a$、周波数軸方向に $b$ だけシフトした**Gabor原子（Gabor Atoms）**の線形結合として数学的に分解します。

\[
g_{m,n}(t) = g(t - ma) e^{j 2\pi n b t}
\]

信号 $x(t)$ は、Gabor係数 $c_{m,n}$ を用いて以下のように表現されます。

\[
x(t) = \sum_{m,n} c_{m,n} g_{m,n}(t)
\]

適切な冗長性（Overcompleteness: $ab < 1$）を持たせることで、過渡的なトランジェント成分（インパルス）と持続的なハーモニック成分（倍音）を高い幾何学的な直交性をもって時間-周波数平面上にマッピング（タイリング）することが可能になります。

```
 Gaborタイリング (時間-周波数平面)
  周波数
    ^
    |  +---+---+---+---+
    |  |   |   |   |   |  <- 高域ほど時間解像度を高めるか、
    |  +---+---+---+---+     冗長性フレームでマッピング
    |  |     |     |   |
    |  +-----+-----+---+
    |  |           |   |  <- 低域は周波数解像度を極限まで高める
    +-------------------> 時間
```

### 3.2 ベイズ推論とMCMCによるスペクトル平滑化
Gabor係数 $c_{m,n}$ やARパラメータ $\theta$ には、観測ノイズや突発的なノイズによる不確実性が含まれます。
これを階層ベイズモデルに落とし込み、観測データ $y$ が与えられた下での事後確率分布を**ギブス・サンプリング（MCMC）**によってサンプリングし、真のスペクトルエンベロープを推定します。

1. **事前分布の設定**:
   ARパラメータ $\theta$ には平滑性を促す共役事前分布（ガウス分布など）を設定し、ノイズインジケータ $i$ にはベルヌーイ分布を設定します。
2. **ギブス・サンプリングによる反復サンプリング**:
   以下の条件付き事後確率分布から交互にパラメータをサンプリングします。
   - $\theta^{(t)} \sim p(\theta | x^{(t-1)}, y)$
   - $x^{(t)} \sim p(x | \theta^{(t)}, y)$
3. **結果の反映**:
   非リアルタイムの別スレッドでこのサンプリングを行うことで、背景の定常ノイズ（ヒスノイズ）のみを確率的に「ろ過」し、音楽の純粋な倍音成分のみをくっきりと浮き上がらせたスペクトルエンベロープを構築してGUIに受け渡します。

---

## 4. Intel GPU（統合GPU）環境におけるDirect2D描画の最適化

Intel Coreプロセッサに搭載されている統合GPU（Iris Xeなど）は、ビデオ専用の超高速VRAMを持たず、メインシステムメモリ（RAM）の一部を共有（Unified Memory）して動作します。

### 4.1 CPU-GPU間の同期ボトルネック
JUCEの標準的な2D描画エンジンでは、画面の書き換えのたびに以下の処理が発生します。
1. **CPUでのソフトウェアレンダリング（バックアップイメージの保持）**
2. **メインメモリからGPU共有メモリ領域へのテクスチャデータの転送（PCIeバス/内部メモリバス経由のコピー同期）**
3. **GPUによる画面表示**

アナライザーのように、毎秒60フレームで数千点以上のスペクトルデータを描画する場合、この転送と同期処理がメインメモリ帯域を奪い合い、音声処理スレッドに割り込み（CPUスパイク）を発生させる致命的な原因となります。

### 4.2 `setBackupEnabled(false)` の物理的メリット
JUCE 8のDirect2Dレンダラーにおいて、イメージに対して `setBackupEnabled(false)` を呼び出すと、**「CPU側での描画バックアップデータの維持（ミラーリング）」が完全にスキップ**されます。

- **GPUオンリーテクスチャ描画**:
  描画データはGPUメモリ上にのみ配置され、描画コマンドは直接GPUのレンダーコマンドキューへ送られます。これにより、メモリバス経由の不要な同期コピーがなくなり、**バス帯域の競合が完全にゼロ**になります。
- **デバイスロスト時の対処**:
  Direct3D/Direct2Dのコンテキストロスト（ディスプレイの接続変更やスリープ復帰時など）が発生した場合は、画像はクリアされますが、アナライザーは毎フレーム動的にスペクトルを描画し直しているため、単に次のフレームで画像を再描画すればよく、データ消失のデメリットは実質的に皆無です。
